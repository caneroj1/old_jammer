.container-fluid
	.row
		.col-xs-4.left-info.well#before-message
			%h1.text-center Messages
			%ul.list-unstyled
				- @messages.each do |msg|
					%li.text-center.msg-info-row
						%table
							%tr
								%td.left-cell
									%h3.subject= link_to msg.subject.nil? ? "Subject" : msg.subject, messages_musician_path(@musician, m: 12)
									%p.sender= "From: #{msg.sender}"
									%p.date= "Sent: #{msg.created_at.strftime("%d/%-m/%Y")}"
								%td.msg-pic= image_tag msg.sender_picture, class: "img-circle msg-pic"
		.col-xs-8.left-info
			- if @message
				= render partial: "partials/message/message"