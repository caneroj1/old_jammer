%h2.text-center
	= @message.subject
	%small= "last message: #{@message.updated_at.strftime("%d/%-m/%Y")}"
%h4.text-center= "#{@message.type?} from #{@message.sender}"
.col-xs-10.col-xs-offset-1.replies
	.col-xs-9.col-xs-offset-1
	.col-xs-offset-2.col-xs-9
