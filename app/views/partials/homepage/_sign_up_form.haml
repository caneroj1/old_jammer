.modal.fade{ id: "#{sign_up_name}", tabindex: -1, role: "dialog" }
	.modal-dialog
		.modal-content
			.modal-header
				%button.close{ :data => { dismiss: "modal" } }
					%span.glyphicon.glyphicon-remove
					%span.sr-only Close
				%h2.modal-title.text-center Sign Up
			.modal-body
				.row
					= form_for(@resource, as: @resource_name, url: registration_path(@resource_name)) do |f|
						.col-xs-5.col-xs-offset-1
							.form-group
								= f.label :first_name
								= f.text_field :first_name, class: "form-control"
							.form-group
								= f.label :last_name
								= f.text_field :last_name, class: "form-control"
							.form-group
								= f.label :email
								= f.email_field :email, class: "form-control"
							.form-group
								=f.label :password
								= f.password_field :password, class: "form-control"
							= f.submit 'Sign Up', class: "btn btn-info"
							%button.btn.btn-danger{ :data => { dismiss: "modal" } } Cancel
						.col-xs-5
							%p.help-block You don't need to enter your location information, but it would improve searching for nearby musicians.
							.form-group
								= f.label :street
								= f.text_field :street, class: "form-control"
							.col-xs-8.location
								.form-group
									= f.label :city
									= f.text_field :city, class: "form-control"
							.col-xs-4.location
								.form-group
									= f.label :state
									= f.select(:state, @state_list)
							.form-group
								= f.label :instrument
								= f.select(:instrument, @instrument_list)
			.modal-footer
				.row
					.col-xs-12.text-center
						%h5
							Thanks for considering Jammer! We hope to connect you with nearby musicians so you can jam hard and make totally awesome sounds. 