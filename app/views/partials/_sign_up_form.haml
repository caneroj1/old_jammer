.modal.fade{ id: "#{sign_up_name}", tabindex: -1, role: "dialog" }
	.modal-dialog
		.modal-content
			.modal-header
				%button.close{ :data => { dismiss: "modal" } }
					%span.glyphicon.glyphicon-remove
					%span.sr-only Close
				%h2.modal-title.text-center Sign Up
			.modal-body
				.row
					.col-xs-6.col-xs-offset-3
						= form_for(@resource, as: @resource_name, url: registration_path(@resource_name)) do |f|
							.form-group
								= f.label :first_name, for: :f_name
								= f.text_field :first_name, class: "form-control", id: :f_name
							.form-group
								= f.label :last_name, for: :l_name
								= f.text_field :last_name, class: "form-control", id: :l_name
							.form-group
								= f.label :email, for: :email
								= f.email_field :email, class: "form-control", id: :email
							.form-group
								=f.label :password, for: :password
								= f.password_field :password, class: "form-control", id: :password
							= f.submit 'Sign Up', class: "btn btn-info"
							%button.btn.btn-danger{ :data => { dismiss: "modal" } } Cancel
			.modal-footer
				.row
					.col-xs-12.text-center
						%h5
							Thanks for considering Jammer! We hope to connect you with nearby musicians so you can jam hard and make totally awesome sounds. 